<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.arknews"
    android:installLocation="auto">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:fullBackupContent="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ARKNews">
        <activity android:name=".ui.settings.NotificationActivity" />
        <activity android:name=".ui.help.AboutActivity" />
        <activity android:name=".ui.favourite.FavoriteChannelsActivity" />
        <activity android:name=".ui.settings.AutoDelActivity" />
        <activity android:name=".ui.help.FAQActivity" />
        <activity android:name=".ui.settings.SettingsActivity" />
        <activity android:name=".ui.home.HomeActivity" />
        <activity android:name=".ui.news_article.HistoryActivity" />
        <activity android:name=".ui.news_article.ArticleActivity" />
        <activity android:name=".ui.news_article.PinnedActivity" />
        <activity android:name=".ui.categories.CategorySelectionActivity" />
        <activity
            android:name=".ui.SplashActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>